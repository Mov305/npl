import { handleSubmit } from "../src/client/js/formHandler"

describe('to describe the handleSubmit function',()=>{
    test('to test the handleSubmit function', ()=>{
        expect(handleSubmit).toBeDefined()
    })
})